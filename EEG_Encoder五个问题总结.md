# M3ANET EEG Encoder：五个问题总结

## EEG Encoder 总流程

```text
EEG输入 [B,128,29184]
→ Transpose [B,29184,128]
→ BatchNorm1d(29184)
→ Transpose [B,128,29184]
→ Chebyshev GCN（K=3）
→ ReLU
→ Conv1D(128→64,k=8,s=4)
→ ChannelwiseLayerNorm(64)
→ Conv1D(64→64,k=1)
→ ResBlock(64→64)
→ ResBlock(64→128)
→ ResBlock(128→128)
→ Conv1D(128→64,k=1)
→ EEG特征 [B,64,270]
```

---

## 1. 为什么输入后进行 Transpose → BatchNorm → Transpose？

原始输入为：

```text
[B,128,29184]
```

`BatchNorm1d` 将第二维视为通道维。源码使用：

```python
self.BN1 = nn.BatchNorm1d(29184)
spike = self.BN1(spike.transpose(1, 2)).transpose(1, 2)
```

因此需要先转置：

```text
[B,128,29184]
→ [B,29184,128]
```

此时29184个时间位置被当作29184个通道。对于每个时间位置 `t`，BatchNorm 使用所有 batch 样本和128个电极计算均值与方差：

\[
\mu_t=\operatorname{Mean}_{b,e}(X_{b,t,e})
\]

\[
y_{b,t,e}=\gamma_t\frac{X_{b,t,e}-\mu_t}{\sqrt{\sigma_t^2+\epsilon}}+\beta_t
\]

归一化后再次转置，恢复 GCN 所需的电极维：

```text
[B,29184,128]
→ [B,128,29184]
```

### 参数量

每个时间位置有一个缩放参数和一个偏移参数：

\[
29184\times2=58368
\]

此外还有29184个运行均值和29184个运行方差，但它们不是可学习参数。

### 需要注意

这种设计并不常见，因为它：

- 将绝对时间位置作为 BatchNorm 通道；
- 混合不同电极计算统计量；
- 强制输入长度固定为29184；
- 为每个时间位置学习独立的缩放和偏移。

更常见的 EEG 处理方式是直接使用 `BatchNorm1d(128)`，让每个电极独立归一化。因此，这可能是作者的特殊实验设计，也可能是维度处理上的实现选择，不能将其视为标准做法。

---

## 2. 为什么 GCN 后还要经过 ReLU、Conv1D、LayerNorm 和 Conv1D？

### GCN：提取电极空间关系

GCN 利用可学习邻接矩阵，聚合不同 EEG 电极之间的信息。它主要处理电极维的空间关系。

### ReLU：引入非线性

GCN 的基本计算主要是线性变换：

\[
Y=\sum_kW_kT_k(L)X
\]

经过 ReLU：

\[
\operatorname{ReLU}(x)=\max(0,x)
\]

可以增加非线性表达能力。否则连续的线性层整体上仍然相当于一次线性变换。

### Conv1D(128→64,k=8,s=4)：时间特征提取和降采样

```text
[B,128,29184]
→ [B,64,7295]
```

它同时完成：

- 利用连续8个采样点提取局部时间特征；
- 融合128个电极通道；
- 将通道数从128压缩到64；
- 通过步幅4降低时间长度和计算量。

### ChannelwiseLayerNorm：稳定特征尺度

它对每个样本、每个时间位置的64个通道进行归一化：

\[
\mu_{b,t}=\operatorname{Mean}_c(X_{b,c,t})
\]

它不依赖 batch，训练和推理时计算方式相同。

### Conv1D(64→64,k=1)：通道投影

该卷积不改变时间长度，只在每个时间位置重新组合64个通道：

\[
y_t=Wx_t+b
\]

### 为什么不直接把 GCN 输出送入 ResBlock？

理论上可以，但需要重新设计后续结构。当前设计先完成：

```text
GCN：空间关系
→ k=8 Conv1D：局部时间关系和降采样
→ LayerNorm：稳定数值尺度
→ k=1 Conv1D：通道重新组合
→ ResBlock：进一步提炼特征
```

如果直接进入当前 ResBlock：

- 时间长度29184过长，计算量较大；
- ResBlock 内部卷积核均为1，本身不直接融合相邻时间点；
- 缺少进入深层网络前的时间特征提取和通道压缩。

---

## 3. 为什么要串联三个 ResBlock？只使用一个可以吗？

三个 ResBlock 的形状变化为：

```text
[B,64,7295]
→ ResBlock(64→64)
[B,64,2431]
→ ResBlock(64→128)
[B,128,810]
→ ResBlock(128→128)
[B,128,270]
```

### 作用一：逐级降低时间长度

每个 ResBlock 最后都有 `MaxPool1d(3)`：

```text
7295 → 2431 → 810 → 270
```

三个池化层累计约降采样27倍。

### 作用二：逐级提取更高级特征

随着序列缩短，后层的一个时间位置对应原始输入中更大的时间范围，可以形成更抽象的 EEG 表示。

### 作用三：增加通道容量

```text
64 → 64 → 128 → 128
```

时间分辨率降低时增加通道数，使网络能够保存更多不同类型的抽象特征。

### 只使用一个可以吗？

可以重新设计，但不能直接删除后两个。只保留一个时输出为：

```text
[B,64,2431]
```

而当前后续模块按照长度270设计，会出现尺寸不匹配。可以用更大的步幅一次降采样，但可能导致信息损失过于突然。最终效果需要消融实验验证。

---

## 4. 为什么 ResBlock 后还要使用 1×1 Conv1D？

三个 ResBlock 输出：

```text
[B,128,270]
```

最后经过：

```text
Conv1D(128→64,k=1)
```

得到：

```text
[B,64,270]
```

它的作用是：

- 将128个通道压缩为64个通道；
- 对128个通道进行加权组合；
- 保持时间长度270不变；
- 使 EEG 特征通道数与后续 CMCA 的64通道相匹配。

对每个时间位置，它相当于一个全连接投影：

\[
y_t=Wx_t+b,\qquad W\in\mathbb{R}^{64\times128}
\]

所以它负责通道投影和降维，不负责提取相邻时间点的信息。

---

## 5. ResBlock 的内部结构是否固定？

不是固定的。

经典 ResNet BasicBlock 通常是：

```text
Conv
→ BatchNorm
→ ReLU
→ Conv
→ BatchNorm
→ 残差相加
→ ReLU
```

M3ANET 使用的是参考 WaveRNN 和 RawNet 改造的一维 ResBlock：

```text
主分支：
Conv1D(k=1)
→ BatchNorm
→ PReLU
→ Conv1D(k=1)
→ BatchNorm

残差分支：
输入直接连接
或 Conv1D(k=1) 匹配通道数

主分支 + 残差分支
→ PReLU
→ MaxPool1D(k=3,s=3)
```

各层作用：

- `Conv1D`：学习通道组合和特征变换；
- `BatchNorm`：稳定中间特征分布；
- `PReLU`：引入非线性，并允许保留一部分负值；
- 残差连接：保留输入信息，改善梯度传播；
- 残差支路的 `1×1 Conv`：在通道数变化时匹配形状；
- `MaxPool1D`：降低时间长度。

`MaxPool` 并不是标准 ResBlock 必须包含的部分。经典 ResNet 更常使用步幅为2的卷积降采样。M3ANET 的 ResBlock 是根据 EEG 编码需求定制的结构。

这些设计并非完全随意，每个组件都有明确作用；但具体使用几层、通道数多少、池化放在哪里，仍属于经验性架构设计，最终必须通过实验和消融研究验证。

---

## 最终理解

```text
输入 BatchNorm
：稳定原始EEG尺度，但本代码按时间点归一化较特殊

GCN
：提取电极之间的空间关系

ReLU
：增加非线性

k=8 Conv1D
：提取局部时间特征并降采样

ChannelwiseLayerNorm
：稳定每个时间位置上的通道特征

k=1 Conv1D
：重新组合通道

三个 ResBlock
：逐级提炼特征、增加通道并压缩时间长度

最终 k=1 Conv1D
：将128通道投影为后续模块需要的64通道
```
